const CONFIG_SECTION = "myGitPhpFormatter";
const OUTPUT_CHANNEL_NAME = "My Git PHP Formatter";

const COMMANDS = {
  formatChangedFiles: "myGitPhpFormatter.formatChangedFiles",
  formatCurrentFile: "myGitPhpFormatter.formatCurrentFile",
};

module.exports = {
  COMMANDS,
  CONFIG_SECTION,
  OUTPUT_CHANNEL_NAME,
};
